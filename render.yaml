services:
  - type: web
    name: vibecut-pro
    runtime: node
    plan: free
    buildCommand: npm install --production=false && npm run build
    startCommand: NODE_ENV=production node server/server.cjs
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: APP_PASSWORD
        sync: false
